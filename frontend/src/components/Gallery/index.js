// Import the ImageGallery component from the ImageGallery.js file within the same directory
import ImageGallery from './ImageGallery';

// Export the ImageGallery component for use in other parts of the application
export { ImageGallery };

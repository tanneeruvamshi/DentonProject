import React from 'react';
import MenuItem from './MenuItem'; // Import the MenuItem component
// import MenuSection from './MenuSection'; // Import the MenuSection component

// Define the Menu component
const Menu = () => (
  <div>
    <MenuItem />
    {/* <MenuSection /> */}
  </div>
);

export default Menu; // Correctly export the Menu component
